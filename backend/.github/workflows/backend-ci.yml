name: Backend CI

on:
  push:
    branches:
      - backend
      - main
  pull_request:
    branches:
      - backend
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore backend/Rentix.sln

      - name: Build
        run: dotnet build backend/Rentix.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test backend/Rentix.sln --no-build --configuration Release --verbosity normal